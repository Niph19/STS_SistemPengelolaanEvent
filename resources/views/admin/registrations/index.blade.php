<x-layouts.dashboard :title="'Peserta Event'"><div class="space-y-5"><div><a href="{{ route('admin.events.show', $event) }}" class="text-sm text-ink-secondary">&larr; Detail event</a><h2 class="text-xl font-semibold mt-2">{{ $event->title }}</h2></div><form method="GET" action="{{ route('admin.events.registrations', $event) }}" class="flex gap-3"><input name="search" value="{{ request('search') }}" placeholder="Cari peserta..." class="flex-1 px-4 py-2.5 rounded-lg bg-navy-surface border border-navy-border"><select name="status" onchange="this.form.submit()" class="px-4 py-2.5 rounded-lg bg-navy-surface border border-navy-border"><option value="">Semua status</option>@foreach(['pending','approved','rejected','canceled'] as $status)<option value="{{ $status }}" @selected(request('status') === $status)>{{ ucfirst($status) }}</option>@endforeach</select><button class="px-5 py-2.5 bg-coral text-white rounded-lg">Cari</button></form><div class="overflow-x-auto rounded-xl border border-navy-border"><table class="w-full text-sm"><thead class="bg-navy-surface"><tr><th class="p-4 text-left">Peserta</th><th class="p-4 text-left">Tanggal</th><th class="p-4 text-left">Status</th><th class="p-4 text-left">Aksi</th></tr></thead><tbody class="divide-y divide-navy-border">@forelse($registrations as $registration)<tr><td class="p-4">{{ $registration->user->name }}<div class="text-xs text-ink-muted">{{ $registration->user->email }}</div></td><td class="p-4">{{ $registration->registered_at->isoFormat('D MMM YYYY HH:mm') }}</td><td class="p-4">{{ ucfirst($registration->status) }}</td><td class="p-4"><form method="POST" action="{{ route('admin.registrations.update-status', $registration) }}" class="flex gap-2">@csrf @method('PATCH')<select name="status" class="px-2 py-1 rounded bg-navy-surface border border-navy-border"><option value="pending">Pending</option><option value="approved">Approved</option><option value="rejected">Rejected</option><option value="canceled">Canceled</option></select><button class="text-coral">Simpan</button></form></td></tr>@empty<tr><td colspan="4" class="p-10 text-center text-ink-muted">Belum ada pendaftaran.</td></tr>@endforelse</tbody></table></div>{{ $registrations->links() }}</div></x-layouts.dashboard>
