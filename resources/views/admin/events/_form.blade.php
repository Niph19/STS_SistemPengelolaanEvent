@csrf
<div class="grid gap-5">
    <div><label class="block text-sm mb-1">Judul</label><input name="title" value="{{ old('title', $event->title ?? '') }}" required class="w-full px-4 py-2.5 rounded-lg bg-navy-surface border border-navy-border"></div>
    <div><label class="block text-sm mb-1">Deskripsi</label><textarea name="description" rows="5" required class="w-full px-4 py-2.5 rounded-lg bg-navy-surface border border-navy-border">{{ old('description', $event->description ?? '') }}</textarea></div>
    <div class="grid sm:grid-cols-2 gap-5"><div><label class="block text-sm mb-1">Kategori</label><select name="category_id" required class="w-full px-4 py-2.5 rounded-lg bg-navy-surface border border-navy-border">@foreach($categories as $category)<option value="{{ $category->id }}" @selected(old('category_id', $event->category_id ?? '') == $category->id)>{{ $category->name }}</option>@endforeach</select></div><div><label class="block text-sm mb-1">Pengelola</label><select name="pengelola_id" required class="w-full px-4 py-2.5 rounded-lg bg-navy-surface border border-navy-border">@foreach($pengelolas as $pengelola)<option value="{{ $pengelola->id }}" @selected(old('pengelola_id', $event->pengelola_id ?? '') == $pengelola->id)>{{ $pengelola->name }}</option>@endforeach</select></div></div>
    <div class="grid sm:grid-cols-2 gap-5"><div><label class="block text-sm mb-1">Lokasi</label><input name="location" value="{{ old('location', $event->location ?? '') }}" required class="w-full px-4 py-2.5 rounded-lg bg-navy-surface border border-navy-border"></div><div><label class="block text-sm mb-1">Kapasitas</label><input type="number" min="1" name="capacity" value="{{ old('capacity', $event->capacity ?? 1) }}" required class="w-full px-4 py-2.5 rounded-lg bg-navy-surface border border-navy-border"></div></div>
    <div class="grid sm:grid-cols-3 gap-5"><div><label class="block text-sm mb-1">Mulai</label><input type="datetime-local" name="start_date" value="{{ old('start_date', isset($event) ? $event->start_date->format('Y-m-d\\TH:i') : '') }}" required class="w-full px-4 py-2.5 rounded-lg bg-navy-surface border border-navy-border"></div><div><label class="block text-sm mb-1">Selesai</label><input type="datetime-local" name="end_date" value="{{ old('end_date', isset($event) && $event->end_date ? $event->end_date->format('Y-m-d\\TH:i') : '') }}" class="w-full px-4 py-2.5 rounded-lg bg-navy-surface border border-navy-border"></div><div><label class="block text-sm mb-1">Status</label><select name="status" required class="w-full px-4 py-2.5 rounded-lg bg-navy-surface border border-navy-border">@foreach(['upcoming'=>'Akan datang','ongoing'=>'Berlangsung','completed'=>'Selesai','canceled'=>'Dibatalkan'] as $key => $label)<option value="{{ $key }}" @selected(old('status', $event->status ?? 'upcoming') === $key)>{{ $label }}</option>@endforeach</select></div></div>
</div>
